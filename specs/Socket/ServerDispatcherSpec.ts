
describe('Socket.ServerDispatcher', () => {
	/* tslint:disable */
	xit('should recieve clientId for connect new socket', () => {

	});

	xit('should dispatch action from socket to server dispatcher', () => {

	});

	xit('should not dispatch action from socket back to socket', () => {

	});

	xit('should unbind action dispatching on disconnect', () => {

	});

	xit('should rebind action dispatching on reconnect', () => {

	});

	xit('should write error of socket to console', () => {

	});

	xit('should dispatch action only to target clientId if specified', () => {

	});
	/* tslint:enable */
});
